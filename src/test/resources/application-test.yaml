spring.cache.type: none
spring:
  sql:
    init:
      mode: always
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:jira;NON_KEYWORDS=VALUE
    username: sa
    password: sa
  liquibase:
    changeLog: "classpath:db/test.sql"
